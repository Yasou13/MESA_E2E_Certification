# E4 — Run Lifecycle, Freeze, and Evidence Integrity

PHASE: E4

STATUS: GREEN

BASE SHA: `cbf83ad`

PHASE COMMIT: commit containing this report (`e2e-04`)

PROBLEM REPRODUCED: PARTIALLY_CONFIRMED for the prior count: the live run
contains nine, not ten, JSON control artifacts with p8b02 IDs inside the p8b03
directory. CONFIRMED that no contract freeze/sidecar exists and the run lacks
declared immutable-reuse authorization.

ROOT CAUSE: Lifecycle, freeze verification, run-ID validation, and evidence
index requirements existed as policy but not executable enforcement.

FILES CHANGED: Lifecycle, freeze, and evidence modules; core artifact model;
E4 tests; and an external historical audit record with an original-byte hash
manifest. No historical run file changed.

TESTS ADDED/CHANGED: Invalid lifecycle transition, terminal immutability,
mandatory freeze categories, missing freeze, file/SHA drift, repo SHA drift,
run-ID mismatch, authorized immutable reuse, evidence hash/order/confinement,
and historical-byte preservation.

OLD BEHAVIOR: Invalid state jumps were not prevented; no executable freeze
could detect code/config drift; cross-run artifacts required no provenance;
evidence metadata was not assembled or confined.

NEW BEHAVIOR: Explicit state transitions fail closed; contract freeze covers
repository SHAs, all mandatory material categories, and runtime identities;
mutation returns `INVALIDATED_CODE_CHANGE`; cross-run reuse needs source ID,
reason, exact hash, and authorization; evidence index entries are hashed,
sorted, versioned, and path-confined.

FALSE-PASS CANARIES: Missing freeze, material mutation, repository SHA drift,
run-ID mismatch, incomplete reuse metadata, duplicate index paths, missing
artifacts, and path traversal.

FALSE-FAIL CANARIES: A complete linear lifecycle reaches `PASS_NATIVE`; an
unchanged complete freeze verifies; correctly authorized byte-exact reuse is
accepted.

FREEZE / ORACLE IMPACT: Establishes the freeze and evidence primitives used by
later gates and oracle separation. It does not expose or consume hidden GT.

BACKWARD COMPATIBILITY: Historical evidence remains byte-identical and keeps
its original verdict. A separate audit states that it is not a valid Profile B
v2 result and freezes all observed original hashes.

MESA CONTRACT DEPENDENCY: NONE

METHODOLOGY DECISION REQUIRED: NONE

FOCUSED TEST RESULT: 9/9 E4/core/historical tests passed.

RELEVANT SUITE RESULT: 33/33 pytest tests and optimized 17/17 self-tests passed.

KNOWN LIMITATIONS: Freeze creation requires callers to supply the three exact
repository SHAs and runtime identities; acquiring MESA/MESA_Data SHAs belongs
to a future synchronized run, not this repository-only hardening phase.

WHY SAFE TO CONTINUE: All new mutation checks are fail-closed, generated test
artifacts stay in temporary directories, and historical evidence is verified
from an external audit without any in-place modification.
